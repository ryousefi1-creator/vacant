# Python
.venv/
__pycache__/
*.pyc

# CV model weights (large; downloaded on setup — see README)
models/
*.pt

# research / scratch / heavy media (not part of the product)
work/
results/
source/

# Next.js app
vacant-app/node_modules/
vacant-app/.next/
vacant-app/.vercel/
vacant-app/next-env.d.ts

# OS / editor
.DS_Store
*.log

# local launcher (contains the write token)
start.sh

# TLS key/cert for local HTTPS — never commit private keys
auto.key
auto.crt
*.pem

# aider tool history / cache
.aider*
